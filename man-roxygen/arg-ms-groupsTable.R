#' @param groups data.table with feature groups from correspondence across MS
#' analyses as obtained by the msData method `get_groups()`. Note that
#' correspondence of features across MS analyses is performed with the method
#' `group_features()` from the msData class.
