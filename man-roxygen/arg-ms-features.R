#' @param features A character vector with the feature id/s or a data.frame or
#' data.table with a column named `feature` with the feature id/s. A column
#' named `analysis` can be given to specify from which analyses the features are
#' returned.
