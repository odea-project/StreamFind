#' @param correctSuppression Logical (length 1) with `TRUE` for correcting the intensity for matrix
#' suppression before applying intensity based filters. Note that suppression factor as obtained by
#' the processing method `CorrectMatrixSuppression` must be present. When not available the
#' correction is ignored even if set to `TRUE`.
