#' @param project_dir Path to StreamFind project folder (.sf). DB_Engine will place main.duckdb inside it. If reusing an existing project_dir, DB_Engine will connect to the existing Engine.duckdb file and preserve previous data. If the project_dir does not exist, DB_Engine will create the directory and initialize a new Engine.duckdb file.
