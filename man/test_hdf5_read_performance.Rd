% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{test_hdf5_read_performance}
\alias{test_hdf5_read_performance}
\title{Test HDF5 read performance for matrix data}
\usage{
test_hdf5_read_performance(file_name, dataset_name = "matrix_data")
}
\arguments{
\item{file_name}{Name of the HDF5 file to read}

\item{dataset_name}{Name of the dataset to read (default: "matrix_data")}
}
\value{
List containing the data as columns and performance metrics
}
\description{
This function tests the performance of reading a large matrix from an HDF5 file.
It looks for the "matrix_data" dataset, reads it, and converts it back to an R list
format (list of columns). It measures read time, conversion time, and provides
comprehensive performance metrics.
}
\details{
The function expects an HDF5 file with a "matrix_data" dataset containing a 2D matrix
of double-precision floating point numbers.

Performance metrics returned:
\itemize{
\item read_time_ms: Time to read data from HDF5 file
\item conversion_time_ms: Time to convert HDF5 data to R list format
\item total_time_ms: Total execution time
\item file_size_bytes/mb: Size of the HDF5 file
\item data_size_mb: Size of the data in memory
\item dataset_info: Information about the dataset (dimensions, type, etc.)
}
}
\examples{
\dontrun{
# First create a test file
test_data <- generate_test_data_cpp()
test_hdf5_performance(test_data, "test.h5")

# Then read it back
result <- test_hdf5_read_performance("test.h5")

# Check results
print(paste("Read time:", result$performance$read_time_ms, "ms"))
print(paste("Data dimensions:", paste(result$performance$dimensions, collapse=" x ")))
}
}
