% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class_DB_MassSpecMethod_FilterFeature.R
\name{DB_MassSpecMethod_FilterFeatures_native}
\alias{DB_MassSpecMethod_FilterFeatures_native}
\title{DB_MassSpecMethod_FilterFeatures_native Class}
\usage{
DB_MassSpecMethod_FilterFeatures_native(
  minSN = NA_real_,
  minArea = NA_real_,
  minWidth = NA_real_,
  maxWidth = NA_real_,
  maxPPM = NA_real_,
  minFwhmRT = NA_real_,
  maxFwhmRT = NA_real_,
  minFwhmMZ = NA_real_,
  maxFwhmMZ = NA_real_,
  minGaussianA = NA_real_,
  minGaussianMu = NA_real_,
  maxGaussianMu = NA_real_,
  minGaussianSigma = NA_real_,
  maxGaussianSigma = NA_real_,
  minGaussianR2 = NA_real_,
  maxJaggedness = NA_real_,
  minSharpness = NA_real_,
  minAsymmetry = NA_real_,
  maxAsymmetry = NA_real_,
  maxModality = NA_integer_,
  minPlates = NA_real_,
  onlyFilled = NA,
  removeFilled = FALSE,
  minSizeEIC = NA_integer_,
  minSizeMS1 = NA_integer_,
  minSizeMS2 = NA_integer_
)
}
\arguments{
\item{minSN}{Numeric (length 1) with the minimum signal-to-noise ratio.}

\item{minArea}{Numeric (length 1) with the minimum peak area.}

\item{minWidth}{Numeric (length 1) with the minimum peak width in seconds.}

\item{maxWidth}{Numeric (length 1) with the maximum peak width in seconds.}

\item{maxPPM}{Numeric (length 1) with the maximum ppm deviation.}

\item{minFwhmRT}{Numeric (length 1) with the minimum FWHM in retention time (seconds).}

\item{maxFwhmRT}{Numeric (length 1) with the maximum FWHM in retention time (seconds).}

\item{minFwhmMZ}{Numeric (length 1) with the minimum FWHM in m/z.}

\item{maxFwhmMZ}{Numeric (length 1) with the maximum FWHM in m/z.}

\item{minGaussianA}{Numeric (length 1) with the minimum Gaussian amplitude.}

\item{minGaussianMu}{Numeric (length 1) with the minimum Gaussian mu (retention time).}

\item{maxGaussianMu}{Numeric (length 1) with the maximum Gaussian mu (retention time).}

\item{minGaussianSigma}{Numeric (length 1) with the minimum Gaussian sigma.}

\item{maxGaussianSigma}{Numeric (length 1) with the maximum Gaussian sigma.}

\item{minGaussianR2}{Numeric (length 1) with the minimum Gaussian fit R-squared (0-1).}

\item{maxJaggedness}{Numeric (length 1) with the maximum jaggedness (smoothness, lower is better).}

\item{minSharpness}{Numeric (length 1) with the minimum sharpness (higher is better).}

\item{minAsymmetry}{Numeric (length 1) with the minimum asymmetry factor.}

\item{maxAsymmetry}{Numeric (length 1) with the maximum asymmetry factor (1.0 = symmetric).}

\item{maxModality}{Integer (length 1) with the maximum number of local maxima (1 = single peak).}

\item{minPlates}{Numeric (length 1) with the minimum theoretical plates (chromatographic efficiency).}

\item{onlyFilled}{Logical (length 1) with \code{TRUE} to keep only filled features, \code{FALSE} to keep only non-filled, \code{NA} for no filter.}

\item{removeFilled}{Logical (length 1) with \code{TRUE} to remove filled features.}

\item{minSizeEIC}{Integer (length 1) with the minimum number of EIC data points.}

\item{minSizeMS1}{Integer (length 1) with the minimum number of MS1 data points.}

\item{minSizeMS2}{Integer (length 1) with the minimum number of MS2 data points.}
}
\value{
A \code{DB_MassSpecMethod_FilterFeatures_native} object.
}
\description{
Settings for filtering features in DB_MassSpecResults_NonTargetAnalysis objects based on feature properties.
}
