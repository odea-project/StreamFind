% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_db_methods.R
\name{db_helpers}
\alias{db_helpers}
\alias{query_db.default}
\alias{list_db_tables.default}
\alias{get_db_table_info.default}
\title{Generic DuckDB Methods for DB-backed StreamFind Objects}
\usage{
\method{query_db}{default}(x, sql, params = NULL)

\method{list_db_tables}{default}(x)

\method{get_db_table_info}{default}(x, tableName)
}
\arguments{
\item{x}{An object with a \verb{$db} field containing the path to a DuckDB file (e.g. a \code{Cache}, \code{Engine}, or ad-hoc structure).}

\item{sql}{A character string: the SQL query to execute.}

\item{params}{(Optional) A named or unnamed list of parameters to bind to the SQL query's placeholders (use \verb{?} for positional or \verb{$name} for named placeholders). Pass \code{NULL} if no parameters are needed.}

\item{tableName}{A character string: the name of the table to get information for.}
}
\value{
For \code{query_db}: a data.frame with the result of the query.

For \code{list_db_tables}: a character vector of table names in the database.

For \code{get_db_table_info}: a data.frame of column names, types, and constraints for the table.
}
\description{
Helper S3 methods for running SQL and retrieving tables/info for any object with a \verb{$db} DuckDB field. Intended for use with objects like \code{Cache}, \code{Engine}, or ad-hoc lists with a DuckDB file reference.
}
\details{
These methods work with \emph{any} S3 object containing a valid \code{db} field (filename to a DuckDB file). They are exported and can be used directly on such objects.
}
\section{Table of Contents}{

\itemize{
\item \code{query_db.default}: Run SQL queries with (optional) parameter binding.
\item \code{list_db_tables.default}: List tables in the DB.
\item \code{get_db_table_info.default}: Get column and schema info for a named table.
}
}

\examples{
# NOT RUN: Usage examples
# cache <- Cache(db = "path_to_db.duckdb")
# list_db_tables(cache)
# query_db(cache, "SELECT * FROM Cache WHERE name = ?", params = list("my_name"))
# query_db(cache, "SELECT * FROM Cache WHERE name = $entry_name", params = list(entry_name = "my_name"))
# get_db_table_info(cache, "Cache")
# ad_hoc <- list(db = "path_to_db.duckdb"); class(ad_hoc) <- "AnyClass"; list_db_tables(ad_hoc)
}
